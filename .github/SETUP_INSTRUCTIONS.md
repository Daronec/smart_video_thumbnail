# Инструкция по настройке GitHub Actions

## 📋 Требования

Workflow настроены и готовы к использованию! Дополнительная настройка секретов не требуется.

## 🔑 Секреты

### GITHUB_TOKEN

Предоставляется автоматически GitHub Actions. Настройка не требуется!

## 🚀 Workflow'ы

### 1. CI Workflow (`ci.yml`)

**Триггеры:** Push или PR в ветки `main` или `develop`

**Действия:**

- ✅ Проверка форматирования
- ✅ Анализ кода
- ✅ Запуск тестов
- ✅ Сборка example приложения

**Секреты не требуются**

### 2. Create Tag Workflow (`create-tag.yml`)

**Триггеры:** Push в ветку `main` с изменениями в `pubspec.yaml`

**Действия:**

- Извлекает версию из `pubspec.yaml`
- Создает git тег `v{version}`
- Отправляет тег в GitHub

**Используемые секреты:** `GITHUB_TOKEN` (автоматически)

### 3. Release Workflow (`release.yml`)

**Триггеры:** Push тега соответствующего паттерну `v*.*.*`

**Действия:**

- ✅ Проверка форматирования
- ✅ Анализ кода
- ✅ Запуск тестов
- 🏷️ Создание GitHub Release

**Используемые секреты:** `GITHUB_TOKEN` (автоматически)

**Примечание:** Публикация на pub.dev выполняется вручную (см. раздел "Публикация на pub.dev")

## 📝 Использование

### Автоматический процесс релиза

1. **Обновите версию в `pubspec.yaml`:**

   ```yaml
   version: 0.3.0
   ```

2. **Обновите `CHANGELOG.md`** с описанием изменений

3. **Закоммитьте и отправьте в main:**

   ```bash
   git add pubspec.yaml CHANGELOG.md
   git commit -m "Release v0.3.0"
   git push origin main
   ```

4. **Автоматическое создание тега:**
   - Запускается workflow `create-tag.yml`
   - Создается тег `v0.3.0`
   - Тег отправляется в GitHub

5. **Автоматическое создание GitHub Release:**
   - Workflow `release.yml` запускается по тегу
   - Выполняются тесты
   - Создается GitHub Release

6. **Публикация на pub.dev (вручную):**
   - Перейдите в директорию проекта
   - Выполните: `dart pub publish`
   - Подтвердите публикацию в браузере

### Ручной процесс релиза

Если предпочитаете полностью ручное управление:

1. **Создайте тег вручную:**

   ```bash
   git tag v0.3.0
   git push origin v0.3.0
   ```

2. **Release workflow запустится автоматически** и создаст GitHub Release

3. **Опубликуйте на pub.dev:**

   ```bash
   dart pub publish
   ```

## 📦 Публикация на pub.dev

### Первая публикация

При первой публикации пакета:

1. **Выполните команду:**

   ```bash
   dart pub publish
   ```

2. **Аутентификация через браузер:**
   - Команда откроет браузер
   - Войдите в свой Google аккаунт
   - Разрешите доступ pub.dev
   - Учетные данные сохранятся автоматически

3. **Подтвердите публикацию:**
   - Проверьте информацию о пакете
   - Введите `y` для подтверждения

### Последующие публикации

После первой публикации учетные данные сохраняются локально:

```bash
# Просто выполните команду
dart pub publish

# Подтвердите публикацию
y
```

### Расположение учетных данных

Учетные данные хранятся локально:

- **Windows:** `%APPDATA%\Pub\Cache\credentials.json`
- **Linux/Mac:** `~/.pub-cache/credentials.json`

**Примечание:** Эти учетные данные используются только локально. Автоматическая публикация через GitHub Actions не настроена.

## 🔍 Мониторинг

### Проверка статуса Workflow

- Перейдите: `https://github.com/Daronec/smart_video_thumbnail/actions`
- Просмотрите запущенные/завершенные workflow
- Проверьте логи на наличие ошибок

### Проверка публикации

После успешного выполнения workflow:

1. **pub.dev:** https://pub.dev/packages/smart_video_thumbnail
2. **GitHub Release:** https://github.com/Daronec/smart_video_thumbnail/releases

## ⚠️ Решение проблем

### Workflow не запускается

**Проблема:** После push в main тег не создается автоматически

**Решение:**

1. Убедитесь, что изменили версию в `pubspec.yaml`
2. Проверьте, что push был в ветку `main`
3. Проверьте статус workflow в Actions

### Тег уже существует

**Проблема:** Ошибка "tag already exists"

**Решение:** Удалите тег и создайте заново

```bash
git tag -d v0.3.0
git push origin :refs/tags/v0.3.0
git tag v0.3.0
git push origin v0.3.0
```

### Тесты не проходят в CI

**Проблема:** Workflow падает на этапе тестирования

**Решение:** Сначала запустите тесты локально

```bash
flutter test test/cache_key_test.dart
flutter analyze
dart format --set-exit-if-changed .
```

### Публикация на pub.dev не удалась

**Проблема:** Ошибка при выполнении `dart pub publish`

**Решение 1 - Проблемы с аутентификацией:**

```bash
# Удалите старые учетные данные
# Windows
del %APPDATA%\Pub\Cache\credentials.json

# Linux/Mac
rm ~/.pub-cache/credentials.json

# Повторите публикацию
dart pub publish
```

**Решение 2 - Проблемы с версией:**

- Убедитесь, что версия в `pubspec.yaml` больше текущей на pub.dev
- Проверьте формат версии (должен быть `X.Y.Z`)

**Решение 3 - Проблемы с валидацией:**

```bash
# Проверьте пакет перед публикацией
dart pub publish --dry-run
```

## 🎯 Лучшие практики

1. **Всегда тестируйте локально перед push:**

   ```bash
   flutter test
   flutter analyze
   dart format .
   ```

2. **Используйте семантическое версионирование:**
   - MAJOR: Критические изменения (1.0.0)
   - MINOR: Новые функции (0.3.0)
   - PATCH: Исправления багов (0.2.1)

3. **Обновляйте CHANGELOG.md** перед каждым релизом

4. **Проверяйте пакет перед публикацией:**

   ```bash
   dart pub publish --dry-run
   ```

5. **Следите за выполнением workflow** после push:
   - Перейдите в Actions
   - Убедитесь, что все шаги выполнены успешно

6. **Публикуйте на pub.dev сразу после создания Release:**
   - Это обеспечит синхронизацию версий
   - Пользователи смогут сразу использовать новую версию

## 📚 Ресурсы

- [Документация GitHub Actions](https://docs.github.com/en/actions)
- [Руководство по публикации на pub.dev](https://dart.dev/tools/pub/publishing)
- [Семантическое версионирование](https://semver.org/)
- [Документация dart pub publish](https://dart.dev/tools/pub/cmd/pub-lish)

---

**Статус:** ✅ Workflow настроены и готовы к использованию

**Процесс релиза:**

1. ✅ Автоматическое создание тега (при push в main)
2. ✅ Автоматическое создание GitHub Release (при создании тега)
3. 📝 Ручная публикация на pub.dev (`dart pub publish`)
