# Решения для неподдерживаемых форматов

## 📊 Анализ проблемы

### Текущая поддержка форматов

| Платформа | Backend          | Поддерживаемые форматы                   | Неподдерживаемые      |
| --------- | ---------------- | ---------------------------------------- | --------------------- |
| Android   | FFmpeg 4.4.2     | ✅ ВСЕ (MP4, AVI, MKV, FLV, WMV, и т.д.) | -                     |
| iOS       | AVFoundation     | ✅ MP4, MOV, M4V, 3GP                    | ❌ AVI, MKV, FLV, WMV |
| macOS     | AVFoundation     | ✅ MP4, MOV, M4V, 3GP                    | ❌ AVI, MKV, FLV, WMV |
| Windows   | Media Foundation | ✅ MP4, AVI, WMV                         | ❌ MKV, FLV           |
| Web       | HTML5 Video      | ✅ MP4, WebM, Ogg                        | ❌ AVI, MKV, FLV, WMV |

---

## 🎯 Рекомендуемые решения

### Решение 1: Конвертация видео (РЕКОМЕНДУЕТСЯ)

**Самое простое и эффективное решение** - конвертировать неподдерживаемые форматы в MP4.

#### Преимущества:

- ✅ Работает на всех платформах
- ✅ Не увеличивает размер приложения
- ✅ Лучшее качество и производительность
- ✅ Нулевая сложность для разработчика

#### Инструменты для конвертации:

**1. FFmpeg CLI (Локальная конвертация)**

```bash
# Конвертация AVI в MP4
ffmpeg -i input.avi -c:v libx264 -c:a aac output.mp4

# Конвертация WMV в MP4
ffmpeg -i input.wmv -c:v libx264 -c:a aac output.mp4

# Конвертация MKV в MP4
ffmpeg -i input.mkv -c:v copy -c:a copy output.mp4

# Быстрая конвертация (без перекодирования)
ffmpeg -i input.avi -c copy output.mp4
```

**2. Программная конвертация в Flutter приложении**

Используйте пакет `ffmpeg_kit_flutter` для конвертации внутри приложения:

```yaml
dependencies:
  ffmpeg_kit_flutter: ^6.0.3
```

```dart
import 'package:ffmpeg_kit_flutter/ffmpeg_kit.dart';
import 'package:ffmpeg_kit_flutter/return_code.dart';

Future<String?> convertToMp4(String inputPath) async {
  final outputPath = inputPath.replaceAll(RegExp(r'\.\w+$'), '.mp4');

  final session = await FFmpegKit.execute(
    '-i "$inputPath" -c:v libx264 -c:a aac "$outputPath"'
  );

  final returnCode = await session.getReturnCode();

  if (ReturnCode.isSuccess(returnCode)) {
    return outputPath;
  }

  return null;
}

// Использование
final mp4Path = await convertToMp4('/path/to/video.avi');
if (mp4Path != null) {
  final thumbnail = await SmartVideoThumbnail.getThumbnail(
    videoPath: mp4Path,
  );
}
```

**3. Online конвертеры**

- [CloudConvert](https://cloudconvert.com/) - API для конвертации
- [Online-Convert](https://www.online-convert.com/)
- [Convertio](https://convertio.co/)

---

### Решение 2: Опциональный FFmpeg пакет (БУДУЩЕЕ)

Создание отдельного пакета `smart_video_thumbnail_ffmpeg` для iOS/macOS.

#### Архитектура:

```
smart_video_thumbnail (core)
├── Android: FFmpeg ✅
├── iOS/macOS: AVFoundation ✅
└── Windows/Web: Native ✅

smart_video_thumbnail_ffmpeg (optional)
└── iOS/macOS: FFmpeg через ffmpeg-kit
```

#### Использование:

```yaml
dependencies:
  smart_video_thumbnail: ^0.4.0
  smart_video_thumbnail_ffmpeg: ^1.0.0 # Опционально
```

```dart
// Автоматически использует FFmpeg если пакет установлен
final thumbnail = await SmartVideoThumbnail.getThumbnail(
  videoPath: '/path/to/video.avi',  // Теперь работает на iOS/macOS
);
```

#### Компромиссы:

**Плюсы:**

- ✅ Поддержка всех форматов на iOS/macOS
- ✅ Опциональная зависимость
- ✅ Не влияет на размер основного пакета

**Минусы:**

- ❌ Увеличение размера приложения (+20-30 MB)
- ❌ Потеря аппаратного ускорения
- ❌ Больше потребление батареи
- ❌ Лицензионные вопросы (LGPL)

**Статус:** Не реализовано. Будет добавлено при наличии спроса.

---

### Решение 3: Web расширение с FFmpeg.wasm (ЭКСПЕРИМЕНТАЛЬНО)

Использование FFmpeg скомпилированного в WebAssembly для поддержки всех форматов в браузере.

#### Архитектура:

```
smart_video_thumbnail_web_extended
└── Web: FFmpeg.wasm для AVI, WMV, MKV, FLV
```

#### Компромиссы:

**Плюсы:**

- ✅ Поддержка всех форматов в браузере
- ✅ Работает offline
- ✅ Нет серверной зависимости

**Минусы:**

- ❌ Очень большой размер загрузки (~30 MB)
- ❌ Медленная работа (в 10-20 раз медленнее нативного)
- ❌ Требует много памяти
- ❌ Не работает в некоторых браузерах (нужен SharedArrayBuffer)
- ❌ Сложная настройка CORS headers

**Статус:** Не рекомендуется. Используйте серверную конвертацию вместо этого.

---

### Решение 4: Серверная конвертация для Web

Загрузка видео на сервер, конвертация в MP4, возврат клиенту.

#### Архитектура:

```
Client (Web) → Upload video → Server (FFmpeg) → Convert to MP4 → Return MP4 → Extract thumbnail
```

#### Компромиссы:

**Плюсы:**

- ✅ Быстрая конвертация (нативный FFmpeg)
- ✅ Поддержка всех форматов
- ✅ Не нагружает клиента
- ✅ Маленький размер клиентского кода

**Минусы:**

- ❌ Требует backend сервер
- ❌ Загрузка больших файлов (медленно)
- ❌ Стоимость серверных ресурсов
- ❌ Проблемы с приватностью
- ❌ Не работает offline

**Статус:** Рекомендуется для enterprise решений.

---

## 📋 Рекомендации по выбору решения

### Для мобильных приложений (iOS/macOS):

1. **Используйте MP4 формат** - работает везде, быстро, эффективно
2. **Конвертируйте при необходимости** - используйте `ffmpeg_kit_flutter`
3. **Рассмотрите опциональный FFmpeg пакет** - если действительно нужны все форматы

### Для web приложений:

1. **ОБЯЗАТЕЛЬНО используйте MP4** - единственный надежный формат
2. **Конвертируйте на сервере** - для enterprise решений
3. **НЕ используйте FFmpeg.wasm** - слишком медленно и тяжело

### Для desktop приложений (Windows):

1. **Используйте MP4 или AVI** - хорошая поддержка
2. **Конвертируйте MKV/FLV** - если нужны эти форматы

---

## 🚀 План развития

### Фаза 1: Документация (✅ ЗАВЕРШЕНО)

- Четкое описание поддерживаемых форматов
- Рекомендации по конвертации
- Примеры использования

### Фаза 2: iOS/macOS FFmpeg пакет (🔜 ПО ЗАПРОСУ)

- Создание `smart_video_thumbnail_ffmpeg`
- Интеграция с ffmpeg-kit
- Опциональная зависимость

### Фаза 3: Web расширение (❓ ВОЗМОЖНО)

- Оценка спроса
- Выбор между FFmpeg.wasm и server-side
- Реализация при необходимости

---

## 💡 Заключение

**Лучшее решение на данный момент:**

- Используйте MP4 формат для максимальной совместимости
- Конвертируйте неподдерживаемые форматы через FFmpeg CLI или `ffmpeg_kit_flutter`
- Дождитесь опционального FFmpeg пакета если действительно нужны все форматы

**Следует принципам:**

- ✅ YAGNI - не добавляем функциональность "на будущее"
- ✅ KISS - простое решение (конвертация) работает лучше всего
- ✅ OCP - архитектура открыта для расширения через опциональные пакеты

---

## 📞 Обратная связь

Если вам нужна поддержка дополнительных форматов, пожалуйста:

1. Создайте issue на GitHub с описанием use case
2. Укажите какие форматы вам нужны
3. Укажите на каких платформах

Это поможет приоритизировать разработку опциональных пакетов.
