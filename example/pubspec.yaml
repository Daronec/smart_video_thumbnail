name: smart_video_thumbnail_example
description: Example app for smart_video_thumbnail plugin
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.10.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  smart_video_thumbnail:
    path: ../
  file_picker: ^8.1.4
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
